import { DefaultTheme } from 'styled-components/dist/types';

const theme: DefaultTheme = {
  colors: {
    primary: '#000',
    secondary: '#fff',
    tertiary: '#555',
    point: '#f8002f',
    line: '#acacac',
  },
};

export default theme;
